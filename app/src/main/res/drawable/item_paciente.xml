<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo (cámbialo si es necesario) -->
    <solid android:color="@color/k3" /> <!-- Ejemplo: blanco -->

    <!-- Bordes redondeados -->
    <corners android:radius="16dp" />

    <!-- Borde visible (usa un color contrastante) -->
    <stroke
        android:width="1dp"
        android:color="@color/k3" /> <!-- Ejemplo: negro -->

    <!-- Padding interno -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>